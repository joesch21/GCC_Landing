# Current state

Snapshot date: 2026-09-06  
Repository: `joesch21/GCC_Landing`  
Branch: `main`  
Current baseline HEAD before this documentation change: `0953749 Fix Vercel opportunity surface routing`

The deployed product is the public Gold Condor site plus GCC research and the GCC Opportunity Surface. `/network` is the canonical dashboard URL and serves `public/opportunity.html`; `/opportunity.html` remains direct static access. `/about` and `/agents` are named public routes. `/network.html` is the legacy/static research detail page.

The current regime data mode is historical/static research. Market inputs are **NOT CONNECTED**. Current Price Environment and LP Environment scores are **WITHHELD**. The opt-in scenario is explicitly synthetic, fixed-fixture data for inspecting deterministic model behavior; it is not a market observation.

The research model is `0.1-research`, experimental and unvalidated. It is not a prediction model, expected-return estimate, or investment recommendation. XAUT remains unresolved and excluded from the scored model.

Agent JSON is publicly available under `/data/agent/` and is generated by `npm run build`. It is versioned/read-only context, not authority. Public JSON does not sign, broadcast, or settle transactions.

Execution status: no transaction execution, private key, wallet signing, contract write, liquidity modification, or live-capital control is present.

Known unresolved work includes historical backtesting of whether regime scores preceded GCC returns or LP-vs-HODL outcomes, replacing unavailable inputs with documented read-only providers, XAUT transmission research, broader multi-asset solver research, and future authority/settlement products outside this repository.
